<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dj.trip.domain.oauth.mapper.OauthMemberMapper">

    <select id="selectOauthMemberByOauthId" parameterType="int">
        select oauth_id, oauth_service_type, email
        from oauth_member
        where oauth_id = #{oauthId}
    </select>

    <select id="selectOauthMemberByOauthServiceTypeAndEmail" parameterType="OauthMember">
        select oauth_id, oauth_service_type, email
        from oauth_member
        where email = #{email} and oauth_service_type = #{oauthServiceType}
    </select>

    <insert id="insertOauthMember" parameterType="OauthMember">
        insert into oauth_member(oauth_service_type, email)
        values (#{oauthServiceType}, #{email})

        <selectKey keyProperty="oauth_id" resultType="int" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>


</mapper>