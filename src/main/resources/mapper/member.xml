<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dj.trip.domain.member.mapper.MemberMapper">

    <select id="insertMember" parameterType="Member">
        insert into member(member_id, password, nickname, email, role)
        values (#{memberId}, #{password}, #{nickname}, #{email}, #{role})
    </select>

    <select id="selectMemberByMemberId" parameterType="String" resultType="Member">
        select member_id, password, nickname, email, role
        from member
        where member_Id = #{memberId}
    </select>

    <select id="selectMemberByOauthServiceTypeAndEmail" parameterType="Member">
        select member_id, oauth_service_type, email
        from member
        where email = #{email} and oauth_service_type = #{oauthServiceType}
    </select>
</mapper>